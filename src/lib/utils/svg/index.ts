/* Function 1.)
Calculates area given a radius ~
  Input: cross-sectional radius
  Output: cross-sectional area in US units 
*/
export function svgDims(height, width) {
  let h = height;
  let w = width;

  const newHeight = 100;
  const newWidth = 100;

  return [newHeight, newWidth];
};
